# 2026-07-09 — StoryFab 全量重构 engagement

## 项目
StoryFab（Tauri 2 + Rust 桌面应用，AI 影视解说）。团队 `software-refactor-storyfab`，主理人=交付总监，成员：架构师(高见远)/工程师(寇豆码)/QA(严过关)。

## 用户已确认决策
- 范围：前端 `src/` + Rust `src-tauri/src/` 全量。
- Rust 保持 **snake_case**（技术硬约束，`mod foo-bar` 编译失败）→ 不重命名 Rust 文件。
- 前端文件名**全 kebab-case，角色后缀拍平**为 `name-role.ts`（如 `commentary-panel-reducer.ts`）。阶段3 改动面 ~107 文件。
- 推进：**先诊断后分阶段执行**；**先补关键测试**再动契约。
- 阶段4+5 选择：**"4+5 完整收尾"**（Stage 4 安全清理 + Stage 5 架构升级），A3 异步并发按用户后续指令继续推进。
- 技术栈：Vite + React 18 + MUI + Tailwind CSS + Tauri 2 + Rust。
- 验证文化：每阶段独立复跑 `cargo check` + `cargo test` / `tsc` + `vitest` + `eslint` + `grep` 契约确认，不依赖单方报告。

## 阶段完成记录

### Stage 4 — Rust 死代码清理 + 文档（b245905）
- 删 `video/processor.rs` 的 `detect_hw_accel()` 方法（零调用）
- 删 `subtitle/types.rs` 的 `WhisperModelInfo` / `WHISPER_LANGS`（无构造/无读取）
- 修 `binary/hw_accel.rs` 不可达 log（`#[cfg]` 分支根因修复，无 `#[allow]`）
- 移除 `Cargo.toml` 未用依赖 `anyhow`
- 补 4 处 `//!` + 若干 `///` 文档缺口
- 验证：cargo check 0/0、cargo test 7 passed（独立核验）

### Stage 5a — 元数据缓存 + 错误统一（7a47a68）
- 新增 `utils/media_cache.rs`（`OnceLock<Mutex<HashMap<(PathBuf, SystemTime), Value>>>` 按 path+mtime 缓存）
- 新增 `utils/error.rs`（`err_msg` / `AppResult<T>`，不改命令签名）
- 路由 `video/processor.rs` + `transcode.rs` / `postprocess.rs` / `cutter.rs` 4 处到缓存版本
- 验证：cargo check 0/0、cargo test 70 passed、lib.rs 零改动

### Stage 5b — LLM Provider 去重（91f819a，+19 / −204）
- 删 `commands/commentary/script_generator/{openai_call, gemini_call, anthropic_call}.rs` 三份重复实现
- 删重复调度器 `call_llm`，`providers.rs` 改纯 re-export 到 `crate::llm::providers`
- `mod.rs` 改调 `call_llm_provider`（行为等价，顺带支持 deepseek/qwen）
- Anthropic 行为分歧：旧 duplicate 将 system_prompt 嵌入 user message + 设 system 字段（双重发送），已采用 canonical 单 channel 行为
- 验证：cargo check 0/0、cargo test 70 passed、lib.rs 零改动

### Stage A3 — 检测并行化（7a2124b，+60 / −21）
- `highlight/combiner.rs`：`get_highlights` 改 async，音频提取后 audio/scene 并发
- `segment/segmenter.rs`：`SmartSegmenter` 加 `#[derive(Clone)]`，`smart_segment` 改 async，energy/scene 并发
- `commands/ai/detection.rs`：`detect_highlights` / `detect_smart_segments` 改 async fn
- 验证：cargo check 0/0、cargo test 70 passed、lib.rs 零改动

### 前端优化 — ESLint 修复（1ae96b6，+37 / −37）
- 修复 `use-reducer-hook.ts` 3 个 `rules-of-hooks` 错误（`createReducerHook` → `useReducerHookFactory`，更新 13 调用点）
- 修复 2 处 `exhaustive-deps` 警告（`dispatch` 加入依赖数组）
- 验证：tsc 0 错、eslint 0 错 25 警告、vitest 738 passed

### O2 — Pipeline Orchestration（1b2008f，8 files：6 new + 2 modified）
- 新增 `commands/commentary/pipeline/{mod,types,commands,director,script,synthesize}.rs`
- 修改 `commands/commentary/mod.rs` + `lib.rs`（仅追加 `run_commentary_pipeline` 注册）
- 实现 `run_commentary_pipeline` 串联 Director → Script → Synthesize
- 事件：`pipeline-progress` / `pipeline-complete` / `pipeline-error`
- 策略：直接调用现有 pub 函数（不走 Tauri invoke），additive-only
- Director 阶段：create → start → generate → approve（P0 自动 approve）
- Script 阶段：构建 `ScriptGeneratorInput` 调用 `generate_commentary_script`
- Synthesize 阶段：lenient 逐段循环，部分失败容忍（`__ERROR__` 标记）
- 验证：cargo check 0/0、cargo test 70 passed、lib.rs 仅 2 行新增

## 提交历史（今日）
```
1ae96b6 fix(frontend): resolve eslint hook errors and exhaustivedeps warnings
7a2124b refactor(rust): parallelize energy/zcr + scene detection (Stage A3)
91f819a refactor(rust): dedupe LLM providers in commentary/script_generator (Stage 5b)
7a47a68 refactor(rust): add media metadata cache + unified error helper (Stage 5a)
b245905 refactor(rust): remove dead code + add module/docs (Stage 4)
463cdc6 chore: update workspace memory log and stage2 spec doc
```

## 未执行（按用户选择跳过）
- A3 异步并发：**已完成**（见上）
- O2 流水线编排：HIGH 风险，触碰契约，明确排除
- 全量 async 重写：HIGH 风险，明确排除
- D1 策略：**先调研再改接现有命令**（不自作主张新增后端命令）。

## 诊断关键结论（架构师）
- `scripts/check-naming.mjs` 是**假门禁**（跳过 core/shared/hooks/pages/...，引用不存在的 docs/NAMING_AND_MODULARIZATION.md）。
- `src-tauri` 实际 Rust 源码 ~94 .rs（其余 ~1930 文件多为 target/ 编译产物，忽略）。
- 隐藏缺陷：
  - D1 `subtitle_extract`/`subtitle_burn_in` 前端在调但 Rust 未注册 → `command not found`；仅 `full-pipeline.ts` 用到（**该文件零外部引用=死代码**，带 @ts-expect-error 半成品）。
  - D2 `check_faster_whisper` 未注册命令，静默失败→Whisper 路径永不生效。
  - D3 `writeTextFile`/`fileExists` 死方法+常量（无 Rust 命令、无调用）；D4 `WINDOW_*` 死常量；D5 `createReducer` vs `createReducerHook` 半完成重构（17 reducer 仅1个用新工厂）；D6 `analysis-report-service.formatDuration` 无意义包装。
- 真实 UI 字幕路径 `subtitle-service.extractSubtitles`（走 ASR）正常，不依赖坏 facade。

## 已定型修复方案
- D1：删死代码 `full-pipeline.ts` → 移除 `subtitle-asr.ts` 的 `extractSubtitles`/`burnInSubtitles` + `invoke.ts` 的 `SUBTITLE_EXTRACT`/`SUBTITLE_BURN_IN` 常量（零 Rust 改动）。
- D2：移除坏 `invoke('check_faster_whisper')`，改为错误驱动 fallback（尝试 Whisper→失败回退 ASR）。

## 阶段路线
0 命名脚本+文档(工程师执行中) → 测试基线(QA) → 1 死代码+契约(D1-D6) → 2 DRY合并 → 3 前端kebab重命名 → 4 Rust仅文档化 → 5 架构优化(可选)。

## 阶段0 完成（已修正）
- 工程师交付：重写 scripts/check-naming.mjs + 新建 docs/NAMING_AND_MODULARIZATION.md / docs/ARCHITECTURE.md。
- 主理人核验并修正两处：① 脚本误将 `utils` 列为禁止目录名（已从 FORBIDDEN_DIRS 移除）；② 角色后缀拍平误含 `*.module.less`/`*.test.ts(x)`，会破坏 Vite CSS Module 与 Vitest 测试发现——已拆分为"工具链后缀(强制保留)"与"语义角色标记(.reducer/.service/.types 可拍平)"。
- 修正后命名基线：文件命名违规 7 + 目录违规 8(PascalCase页目录) + 角色后缀拍平 22 = **37 项待整改**（阶段3 范围，非之前误报的383/也非拍平全部的~107）。
- Vitest include: `src/**/*.{test,spec}.*`；覆盖率门槛仅 5%（测试基线薄，印证先补测试）。
- 关键决策修正：用户原选"角色后缀全拍平"，但 `*.module.less`(CSS Module) 与 `*.test.*`(Vitest) 是工具链契约不可改名——已强制保留，仅拍平无依赖的 reducer/service/types。

## 阶段1 完成（已核验）
- 工程师 software-engineer-2 重执行并真实落盘 D1-D6：删 `full-pipeline.ts` + 改 7 文件（invoke.ts/index.ts/subtitle-asr.ts/file-operations.ts/subtitle-service.ts/whisper-service.ts/analysis-report-service.ts），净删 392 行。
- QA(严过关) 全量回归 + 主理人独立复跑：vitest **41/41 文件 738/738 用例 0 失败**（比741基线少3=删掉的死代码测试，非回退）；`tsc --noEmit` **0 错误**；grep 确认零悬空引用。智能路由 NoOne。
- 3 个新基线测试文件（subtitle-service.test.ts 7例 / whisper-service.test.ts 3 / analysis-report-service.test.ts 5）全绿。

## 阶段2 进行中（DRY 合并）
- 架构师 software-architect-2 产出文件级 spec，存 `docs/stage2-dry-spec.md`。
- 主理人决策：**保留两个工厂**（createReducer 管具名动作状态机；genericUpdateReducer+createAutoSetters 管 bag-of-setters），把 12 个手写 switch reducer 收敛到 createReducer，use-project-edit-state 收敛到 genericUpdateReducer。拒绝"强制单一工厂"（会增加代码=负DRY，违背用户"最少代码量"）。
- 拆分执行（R1/R3 与 R2 共享消费者文件，故顺序执行避免并发改同文件）：
  - Pass1 = R1+R3 已核验：tsc 0 错误，vitest 41/41·738/738 全绿；`src` 外无裸 invoke（仅剩注释提及命令名）；translateText facade 已加并接入 tauri 对象；formatters/project-utils 透传层已删、6 import 已重指。
  - Pass2 = T-R2a+b 已核验：11 个手写 switch reducer 迁移到 createReducer；tsc 0 错误，vitest 41/41·738/738 全绿；`src` 外无裸 invoke，formatDuration 不再从 @/core/video 引入；仅剩 use-project-edit-state.reducer.ts 为手写 switch（Pass3 目标）。
  - Pass3 = T-R2c 已核验：use-project-edit-state 收敛到 genericUpdateReducer+createAutoSetters（删 makeSetter/createProjectEditSetters/ProjectEditSetters/重复 Updater<T>，reducer 文件 168→86 行）；tsc 0、vitest 738 全绿；仅剩 workflow.ts + use-reducer-hook.test.ts 为手写 switch（非目标）。补充：4 个 genericUpdateReducer reducer 的过时 `makeSetter<K>` 文档注释已改为 `createAutoSetters`。Stage2 功能面全绿，待 QA 全量回归。
  - **Stage2 QA 全量回归通过**（鲜眼复跑 41/41·738/738、tsc 0、4 项 grep 证明全过），智能路由 NoOne。Stage2 闭环。累计净删 ~420 行（D1-D6 + R1/R3/R2）。
- 关键风险 G1：createReducer 是 payload-wrapper，手写 reducer/测试/消费者是平铺字段，迁移需三处联动改（reducer+动作类型+每个 dispatch literal/测试）→ 遗漏会编译过但运行时 payload=undefined。G2 RESET 需返回 initialXxxState 引用；G4 whisper-service.test 断言 transcribe_audio 对象形参，facade 必须同形；G6 export-service 保留 executeRequest+try/catch 吞错。

## 阶段3 前端 kebab 重命名
- Pass3a（8 个 PascalCase 页目录 → kebab）**已完成并核验**：tsc 0 错误、vitest 41/41·738/738 全绿、grep 确认零悬空 PascalCase 路径。提交 7f3d2df（顺带将前序未提交 backlog：reducers/app.tsx/check-naming.mjs/docs 一并入库）。
- **踩坑记录（重要）**：Pass3a 首轮工程师只改了目录名却漏改多处 import 路径（动态 import、跨文件相对路径、Highlights/VideoExport 子文件内部路径），且 `tsc` 走增量缓存给出 19 假错误。主理人清 .tsbuildinfo 重跑 → 真错误仅 4（集中在 workspace 子树的 `./Highlights/`、`export/VideoExport` 路径串），派 software-engineer-6 精准修 5 处路径串（保留 `Highlights`/`VideoExport` 标识符），独立核验 tsc 0 + vitest 738 双绿。
- **Pass3b 范围（待执行）**：`node scripts/check-naming.mjs` 当前 = 文件命名违规 7 + 目录 0 + 角色后缀拍平 22 = **29 项**。
  - 7 文件命名违规（kebab）：`keyboard-shortcuts-help/KeyboardShortcutsHelp.module.less`→`keyboard-shortcuts-help.module.less`；`useOriginalEditor.reducer.test.ts`→`use-original-editor.reducer.test.ts`；`useSubtitleExtractor.reducer.test.ts`→`use-subtitle-extractor.reducer.test.ts`；`useVideoProcessingController.reducer.test.ts`→`use-video-processing-controller.reducer.test.ts`；`useAutoSetters.test.ts`→`use-auto-setters.test.ts`；`useAutoSetters.ts`→`use-auto-setters.ts`；`useReducerHook.ts`→`use-reducer-hook.ts`。
  - 22 角色后缀拍平：`*.reducer.ts`→`*-reducer.ts`（含 commentary-panel-reducer/ai-visualizer-reducer/clip-rippling-reducer/highlights-reducer/video-upload-reducer/use-export-handlers-reducer 等）、`*.service.ts`→`*-service.ts`（audio/script/session/voice-catalog-service）、`*.types.ts`→`*-types.ts`（highlight-types）。
  - **关键风险**：`useAutoSetters`/`useReducerHook` 既是文件名又是导出的标识符 → 改名只动"路径末段"，绝不动 `import { useAutoSetters }` 里的标识符。必须用"仅改 import 字符串字面量中路径末段"的脚本，不能用全局字符串替换。
  - 完成标准：命名门禁 0/0/0 + tsc 0 + vitest 738 全绿，再 commit。
  - **Pass3b 已完成并独立核验**（software-engineer-7，提交 69bcecc）：29 文件全 `git mv` + specifier-only 脚本同步 52 文件引用（仅改 import 路径末段，绝不动 `useAutoSetters`/`useReducerHook` 标识符——实测这两个旧 token 在 src 中已无残留路径引用）；命名门禁 **0/0/0**、tsc 0、vitest 738 全绿；主理人独立复跑三项均过。`.module.less` 与 `.test.ts` 工具链后缀保留未动。踩坑：2 个 reducer 文件（video-player/video-selector）被拍平后上移一级目录，脚本漏改子目录消费者的相对深度→工程师手改 5 处（`../xxx-reducer`）。**Stage3 闭环**（3a 目录 + 3b 文件均达标）。

## 阶段4 + 阶段5（Rust 侧，用户选 "4+5 完整收尾"）
- 环境已确认：cargo 1.96.0 可用，`src-tauri/target/` 有缓存（增量编译可行），`Cargo.lock` 已解析 → `cargo check`/`cargo test` 可作门禁。
- 架构师 software-architect 产出计划 `docs/stage4-5-arch-plan.md`：命令图 57 命令全接入 `generate_handler!`，9 个 lib.rs 模块全存活无孤儿。
- **阶段4（低风险，已派 software-engineer-8）**：5 项死代码 + 文档缺口。死代码= `video/processor.rs:111-127 detect_hw_accel()`(零调用)、`subtitle/types.rs:36-41 WhisperModelInfo`(无构造)、`subtitle/types.rs:52-59 WHISPER_LANGS`(无命令返回)、`binary/hw_accel.rs:122` 不可达 log、`Cargo.toml anyhow`(零使用)；文档=4 处 `//!` 缺失 + 若干 `///` 缺失。门禁 cargo check 0错0警 + cargo test；提交信息 "refactor(rust): remove dead code + add module/docs (Stage 4)"。
- **阶段5（架构升级，用户最关心的"架构升级"）执行批次**（每步 cargo check 门禁，主理人独立复跑）：
  - 批次5a（软件工程师）：A2 元数据缓存（path+mtime→Mutex<HashMap> 路由 ffprobe/metadata，性能赢面最大，LOW-MED，不碰契约）+ O3 统一错误 `utils/error.rs`（保守版不改命令签名，顺带移除 anyhow）。
  - 批次5b（软件工程师）：O1 去重 LLM Provider（两套并行 provider `crate::llm::providers` vs `commands/commentary/script_generator/*` + 两套 call_llm + 两套 ScriptStyle/ScriptSegment 类型 → 复用前者删重复，MED，不碰契约）。
  - A3 异步并发（energy/zcr 与 scene 可并发）= 可选低优先级，默认跳过。
  - **明确排除（高风险/超范围）**：O2 后端流水线编排器（改命令契约 HIGH）、全量 async 重写、命令返回改 thiserror 枚举、任何 Rust 文件/目录改名、改 `generate_handler!`。
- 前端侧：Rust 内部重构不碰命令名/签名 → 前端 vitest 不受影响（invoke 已 mock），故 Rust 门禁以 cargo check + cargo test 为准，不强制跑前端 vitest。

## 阶段4 + 5 完成记录（Rust 侧，已全部主理人独立核验）
- **Stage 4 已闭环**（提交 b245905）：5 项死代码全删 + 4 处 `//!` + 若干 `///` 文档缺口补齐。cargo check 0错0警、cargo test 7 passed/0 failed（doc-tests 2 ignored）、grep 确认死符号已移除；`binary/hw_accel.rs:122` 不可达 log 用 `#[cfg(target_os="macos")]`/`#[cfg(not(...))]` 包裹 VideoToolbox/CPU 分支根因修复（无 `#[allow]`）。git 范围仅 src-tauri/。
- **Stage 5a 已闭环**（提交 7a47a68）：A2 元数据缓存 `utils/media_cache.rs`（`OnceLock<Mutex<HashMap<(PathBuf,SystemTime),Value>>>` keyed by path+mtime，锁在阻塞 ffprobe 前释放；`probe_metadata_cached`/`probe_duration_cached` 路由 video/processor.rs + transcode/postprocess/cutter 4 处调用）+ O3 统一错误 `utils/error.rs`（`err_msg`/`AppResult<T>`，不用 thiserror/anyhow，不改命令签名）。cargo check 0/0、cargo test **70 passed/0 failed**、`generate_handler!` 字节不变（lib.rs 不在提交）。
- **Stage 5b 已闭环**（提交 91f819a，+19/−204）：O1 去重 LLM Provider —— 删 `commands/commentary/script_generator/{openai_call,gemini_call,anthropic_call}.rs` 三份重复实现 + 第二调度器 `call_llm`，`providers.rs` 改为纯 re-export（`get_default_model`/`get_http_client` from client + 4 canonical symbols from `crate::llm::providers`），`mod.rs:47` 改调 canonical `call_llm_provider`。cargo check 0/0、cargo test 70/0、lib.rs 无变更。**关键差异处理**：重复版 `call_anthropic` 把 system_prompt 同时塞进 user 消息体 AND 设 system 字段（冗余双发，是脏代码），canonical 仅走专用 system 字段（与 `commands/llm.rs` 生产路径一致）→ 采用 canonical 更干净、非回归；`call_llm`→`call_llm_provider` 调度器等价（provider 字符串规范化等价、OpenAI base_url 默认值相同，model 已在调用前解析为具体串故传 `Some(model)`），额外 bonus：deepseek/qwen 路由现在可用。`ScriptStyle`/`ScriptSegment` 类型按架构师指示保留不合并。
- **Rust 侧整体成果**：3 提交（b245905/7a47a68/91f819a）累计净删 ~225 行（含 -204 去重）；全量 cargo check 0错0警 + cargo test 70 passed/0 failed；`generate_handler!` 契约 0 变更；Rust 文件/目录 0 改名（snake_case 硬约束严守）。前端 vitest 未受影响（命令名/签名不变）。
- **A3 异步并发（energy/zcr 与 scene 并发）** = 可选低优先级，用户未要求 → 默认跳过，未执行。
- **团队收尾**：TeamDelete `software-refactor-storyfab-09ef`。全量重构 engagement 完成。

## O2 后端 + 前端（用户选"继续推进 O2"）
- **O2 PRD**（许清楚）：`docs/o2-pipeline-orchestration-prd.md`。关键发现：`run_ai_director_plan` 无前端调用方，前端真实走 Director 状态机命令序列。
- **O2 架构**（高见远）：`docs/o2-pipeline-architecture.md` + `docs/sequence-diagram.mermaid` + `docs/class-diagram.mermaid`。决策：直接函数调用（不走 Tauri invoke）、additive-only、`pipeline-progress`/`pipeline-complete`/`pipeline-error` 事件。
- **O2 后端实现**（寇豆码，commit `1b2008f`）：6 新文件（`commands/commentary/pipeline/{mod,types,commands,director,script,synthesize}.rs`）+ 修改 `commentary/mod.rs` + `lib.rs` 追加 2 行注册。cargo check 0/0、cargo test 70/0、lib.rs diff 仅新命令。
- **O2 前端实现**（寇豆码，commit `44507f2`）：2 新文件（`pipeline-service.ts` + `use-commentary-pipeline.ts`）+ 7 修改（`types/script.ts`、`command-types.ts`、`invoke.ts`、`methods/commentary.ts`、`tauri/index.ts`、`services/commentary/index.ts`、`commentary-panel.tsx`）。tsc 0 错、eslint 0 错 26 警告（pre-existing）、vitest 738/0。新增 🚀 一键按钮 + 进度条 + 错误提示。
- **最终状态**：Stage 4 + 5（A2/A3/O1/O3）+ 前端 ESLint + O2 后端 + O2 前端，共 **7 次提交**，全部独立核验通过，命令契约零变更。

## 最终收尾（16:30）
- **eslint 彻底清零**（commit `ebe87c0`）：22 个 `exhaustive-deps` 警告全部修复（use-clip-rippling 13 个、workflow-editor 4 个、其余 5 文件各 1 个），通过添加 stable `dispatch`/`dispatchLocal`/`initial` 到依赖数组实现。tsc 0 错、vitest 738/0。剩余 4 个 warning 为 pre-existing（`no-explicit-any` ×3、`no-console` ×1）。
- **构建验证通过**（commit `ce366f9`）：`pnpm tauri build` 成功生成 StoryFab.app。修复 Stage 3 kebab 重命名遗漏的 14 处 less 路径（`script-writing.module.less` 和 `video-composing.module.less` 相对路径错误）。
- **今日总计 9 次提交**，工作区干净，团队已清理。
