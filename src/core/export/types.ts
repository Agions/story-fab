/**
 * @deprecated 请从 @/types 导入导出相关类型
 * 此文件为向后兼容的重导出层，将在后续版本移除
 */
export type {
  ExportFormat,
  ExportQuality,
  ExportResolution,
  ExportConfig,
  ExportResult,
  EncoderSettings,
} from '@/types';

export {
  EXPORT_PRESETS,
  FORMAT_INFO,
} from '@/types';
