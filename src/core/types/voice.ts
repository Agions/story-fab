/**
 * @deprecated 请从 @/types 导入语音/TTS 相关类型
 * 此文件为向后兼容的重导出层，将在后续版本移除
 */
export type {
  TtsBackendName,
  TtsBackend,
  VoiceConfig,
  VoiceInfo,
  SynthesisResult,
  SynthesisProgress,
} from '@/types';
