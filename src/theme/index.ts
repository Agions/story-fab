/**
 * CutDeck Theme System
 *
 * 统一色彩管理和 Ant Design 5 主题配置
 *
 * 收敛说明：
 * - design-system.css — 唯一 CSS 真相来源
 * - colors.ts — TypeScript token + antdTokens
 * - dashboardTokens / videoEditorTokens — 已废弃（0 usage），已移除
 */

export { colors, antdTokens, antdComponentTokens } from './colors';
